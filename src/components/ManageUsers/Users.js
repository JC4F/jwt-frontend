import { useEffect } from 'react';
import { useHistory } from 'react-router-dom';
import './Users.scss';

const Users = (props) => {
    return (  
        <div>
            users components
        </div>
    );
}
 
export default Users;